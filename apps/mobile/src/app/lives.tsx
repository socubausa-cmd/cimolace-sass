import { LiriScreen } from '@/components/liri-screen';

export default function LivesScreen() {
  return <LiriScreen icon="video" title="Lives" subtitle="Cours en direct, replays et programmation des sessions." />;
}
