import React from 'react';
import AnnouncementManager from '@/components/school-life/AnnouncementManager'; // Reusing existing component

// Wrapping it to fit the naming convention if needed, or just re-exporting
export default AnnouncementManager;