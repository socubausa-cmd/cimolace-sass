import React from 'react';
import HorizonHero from '@/components/ui/horizon-hero-section';

// Maquette « Doctrine · Cosmologie » — voyage cosmique recoloré en or (validation avant /t/isna/doctrine).
export default function MaquetteCosmos() {
  return <HorizonHero skipHref="/t/isna" />;
}
