/**
 * Spec JSON Coach Slide (SmartBoard Designer) — source alignée sur
 * `supabase/functions/_shared/coach-slide-agent-complete.json`.
 */
import coachSlideAgentComplete from './coach-slide-agent-complete.json';

export type CoachSlideAgentSpec = typeof coachSlideAgentComplete;

export { coachSlideAgentComplete };
