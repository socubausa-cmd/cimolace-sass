# Exclure tout sauf apps/api/src — Railway n'a besoin QUE du source NestJS
# apps/public-site et apps/app sont sur Vercel
apps/public-site/
apps/app/
apps/med-app/
apps/patient-portal/
apps/worker/
apps/mobile/
.claude/
.wrangler/
tools/
scripts/
# Tous les node_modules (root + nested — Dockerfile fait npm install)
node_modules/
apps/api/node_modules/
apps/api/dist/
# Gros dossiers inutiles
.isna-opus-consolidated/
.git/
.vercel/
dist/
build/
.next/
.turbo/
coverage/
tests/
# Root Supabase CLI dir only (migrations/config) — must NOT match apps/api/src/supabase/
/supabase/
docs/
# Fichiers superflus
*.log
package-lock.json
FUSION_LOG.md
mockup-viewer.html
cloudbuild.api.yaml
.gcloudignore
.env.staging.example
.env.production.example
.env.example

# ── Durcissement (2026-07-26) : `railway up` timeoutait systematiquement a
# l'upload. Le service ne compile QUE apps/api/src — tout le reste est du poids
# mort qui allonge l'upload et le fait echouer.
# ⚠️ MOTIFS ANCRÉS À LA RACINE (/) — OBLIGATOIRE.
# Un motif nu comme `supabase/` exclut TOUT dossier de ce nom, y compris
# `apps/api/src/supabase/` → le build a échoué sur 146 erreurs TS2307
# « Cannot find module '../supabase/supabase.module' » (2026-07-26).
**/node_modules/
/artifacts/
/docs/
/tests/
/supabase/
/precepteur_sources/
*.log
*.mp4
*.pdf
.DS_Store
